# 2026-04-10 — Testsvit, CI/CD och statusgranskning

Session: förmiddag/eftermiddag.

## Vad som gjordes

### CLAUDE.md uppdaterad (tre edits)
- Fas 3 avbockad — Claude Desktop-integration var redan klar sedan 2026-04-09 men inte dokumenterad. Quirk noterad: kräver ny konversation efter att connector lagts till.
- String-tvingning av `doi`/`arxiv_id` tillagd i Designbeslut (saknades sedan commit `eade08f`).
- Nya sektioner: Testplan (14 namngivna testfall) + CI/CD (två workflow-filer + förkrav).

### Testsvit skriven och godkänd
`tests/test_verify_citation.py` — 13 tester, 13/13 godkända, 74% täckning.

Testkategorier:
- Lyckade fall (2): exact match, minor author variation
- Mismatchar (3): title mismatch, year mismatch, not found
- Fuzzy fallback (3): triggered, miss, threshold
- Regressionstester (3): float coercion, hard-errors-only, raw_text
- Venue-varningar (2): arxiv preprint warning, published venue no warning

Teknisk lösning: `_fake_to_thread` async shim ersätter `asyncio.to_thread` i tester — kör synkront utan tråd-overhead. `_get_checker` mockas för att undvika singleton-init. `_fuzzy_fallback` mockas på modul-nivå för kontrollerad testning.

### CI/CD-workflows tillagda
- `.github/workflows/ci.yml` — pytest-matrix 3.10/3.11/3.12 vid push + PR
- `.github/workflows/publish.yml` — twine upload vid `v*`-taggar
- `pyproject.toml` — `[project.optional-dependencies] dev = ["pytest>=8.0", "pytest-cov>=5.0"]`

### Pushad (två commits)
- `e9b3c86` — testsvit + CI/CD + pyproject.toml + CLAUDE.md
- `77fa218` — alla milstolpar avbockade i CLAUDE.md

## Statusgranskning — allt var redan klart

Vid genomgång visade sig alla milstolpar redan vara uppnådda (men odokumenterade):
- Repo: redan publikt
- PyPI: v0.1.0 redan publicerad
- Fas 3 (Claude Desktop): redan registrerad i `claude_desktop_config.json`

CLAUDE.md och Munin uppdaterades för att spegla verkligheten.

## academic-refchecker v3.0.78

Fick release-notis från `markrussinovich/refchecker` (bevakat repo). Uppgraderar inte ännu — Mark Russinovich bekräftade i mejl att upstream är i aktivt flux med LLM hallucination judge och många heuristikuppdateringar. Avvaktar stabilisering. Dependabot sköter PR när det är dags.

## Schemaläggning — misslyckades

Försökte schemalägga en remote agent-check om en vecka via `/schedule`-skill. RemoteTrigger-API:t returnerade 401 Authentication failed — sessionen saknade rätt autentisering mot claude.ai. Lösning: prova igen via https://claude.ai/code/scheduled eller kör `/web-setup` i nytt Claude Code-fönster.
